*.log
*.xo
*link_summary
*weights
*app
*.xml
*.bif
*.BIN
*.o
*.xclbin
*.xsa
*_summary
*.bin
*__pycache__
*.scr
*cpu_sources
*solution1
*.ipcache
*_x
*sd_card
*.Xil
*.tcl
*data
*Work
*.rpt
*img
src/aie_stream/tb/ISS_RPC_SERVER_PORT
*.a
*.db
*.json
*.dat
*.txt
# --- 2026-08-15 additions: simulator/build work dirs and generated state ---
_x_rebuild/
_x*/
Work*/
x86simulator_output*/
aiesimulator_output*/
temp/
.Xil/
sol.db
ISS_RPC_SERVER_PORT
pl_sample_counts
golden_state/
src/aie_stream/tb/data/
src/aie_stream/tb/predicted_*.txt
# native test binaries (sources are tracked)
src/aie_stream/tb/hybrid_golden_steps
src/aie_stream/tb/hybrid_golden_dbg
src/aie_stream/tb/attn_block_aie
src/pl_stream/golden_native
src/pl_stream/golden_hi
src/pl_stream/plhost
src/pl_stream/plhost_timed
# generated weight-header backups
src/attn_block_aie/kernels/weights_orig_backup/
# build reports (regenerated every build)
src/aie_stream/tb/Map_Report.csv
weights_f32/
src/aie_stream/tb/data_float/
# --- 2026-09-18 additions ---
# HLS csynth experiment projects (vitis-run --mode hls), one per flag combination
/csynth*/
# aiesim / profiling run status files
src/aie_stream/tb/*.status
# backups the input generator makes before overwriting its data folder
src/aie_stream/tb/data_int16_bak_*/
# simulator run state, rewritten on every aiesimulator / x86simulator run
src/aie_stream/tb/.AIE_SIM_CMD_LINE_OPTIONS
# board host binary (sources are tracked)
src/pl_stream/plhost_direct
# raw log of the float-layernorm experiment (negative result, feeds no figure; 17 MB)
figs/board_thermal_lockin120_hybFLOATLN_log.csv
# editor settings
.vscode/

# the figure inputs live here and are tracked, despite the patterns above
!data/
!data/**
